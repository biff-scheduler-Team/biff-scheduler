# PLAN-20260922164728 — 红黑榜「生成分享图」

## 目标

用户 2026-09-22：

> 请你给这个我贴过的页面做一个分享按钮 然后弄成分享图片的形式 黑榜红榜默认 TOP10 我贴过的不限场数样式
> 弄好看点吧 底部署名网站 by gaaiyeoi 和 cit忘记怎么拼了 你找找就行

澄清后（同日问答）确定：

1. **署名**：照现网页脚 —— `by @gaaiyeoi 和 by @lcandy2` + 网站 `biff.lcandy.co`
   （**修订 1**：原按现网页脚写成 `@citron`，后经用户指出对齐旧版页脚改为 `@lcandy2`）。
2. **榜单**：「取**总数 / 红榜 / 黑榜**那三个按钮的口径，各取 TOP10」——
   即逐字复用页面三档排序，不另立一套排名规则。
3. **「我贴过的」**：每行给「我贴的那一色 + 全站红黑数」，**不限条数**。

## 范围

**做：**

1. **模型层** `apps/web/src/redblack-poster.ts`（纯逻辑、import 期不碰 DOM、可 node 单测）：
   三榜各 TOP10（走 `redblack.ts::sortByCounts`）+ 我贴过的全部（按全站总数降序，与页面默认档同口径）
   + 头部（届次 / 日期 / 影片数 / 全站三格 / 我的一行小字）+ 底部署名。日期**注入**，不读时钟。
2. **绘制**：1080 宽、`SCALE=2` 超采样、长图过 `SCALE_DOWN_H` 退 1×；深底 + 品牌红条 + 每行一条
   红黑比例条；「我贴过的」行用**贴纸形状**的小圆片当左侧标记（复用 `sticker-shape.ts`）。
3. **共用笔刷** `apps/web/src/poster-brush.ts`：把配色 / 字体 / 圆角 / 超采样 / 退档阈值 / 页脚高 /
   品牌红条从 `poster.ts` 搬出来 —— 两张分享图必须**同一套**（§5 口径单一来源）。
   `apps/web/src/sticker-shape.ts` 同理：贴纸形状（CSS `border-radius` ↔ sprite ↔ 分享图）三处表达里，
   后两处共用同一条 canvas 路径。
4. **复制 / 下载出口** `apps/web/src/components/share-image.ts`：先试复制、失败退化下载并如实提示。
   `PosterPreview.tsx` 改为调用它（原来那份逻辑是内联的）。
5. **弹层** `apps/web/src/components/RedBlackShareDialog.tsx`：复用 S2 `Dialog`（role=dialog / focus trap /
   Esc），只在打开时挂载；预览画布 + 下载 PNG + 复制图片。**焦点归还**由页面负责。
6. **入口**：红黑榜控制行加「生成分享图」按钮（与「只看我贴过」同排 —— 它既不是排序也不是筛选）。
7. **出图是快照**：弹层打开那一刻的数据就是图的内容（打开后再贴一枚不该让图变来变去）。

**不做：**

- **不动页面上的排序 / 筛选 / 卡片呈现**：分享图只是把已有的口径再画一遍。
- **不新造配色**：深底海报配色沿用 `poster.ts` 那套（`COLORS`），只有「黑贴纸在深底上的填充色」
  是新加的一个 token —— 因为页面的 `--rb-black` 是给**浅底画布**配的深色，直接搬进深底海报会变成
  「一枚看不见的黑点」。这一点在 `poster-brush.ts` 里写明了原因。
- **不做 `navigator.share` 系统分享**：仓库至今没有这个能力，也没有需求。
- **不做可配置的 TOP N**：默认 10（常量 `TOP_N`），要改时改一处。
- **不动持久化契约**（`biff.*` 一字未改）。

## 方案取舍

| 备选 | 结论 | 理由 |
|---|---|---|
| 新写一套配色 / 圆角 / 字体 | 否决 | 同一个应用出的两张分享图必须像一家人；抽 `poster-brush.ts` 后两张图共用同一份笔刷 |
| 三榜判据另写一套 | 否决 | 顺序与页面对不上就是 bug；逐字复用 `sortByCounts` + `sortMetric` |
| 榜里列出「该榜为 0」的片 | 否决 | 黑榜里列一堆「黑 0」毫无意义。按**每榜各自的指标**筛（`sortMetric`），不是笼统的「有没有票」 |
| 「我贴过的」也截断到 10 | 否决 | 用户明确「不限场数」 |
| 每行带海报缩略图 | 否决 | 40+ 行要几十张图，出图慢且长；榜单靠数字与比例条就够读 |
| 弹层与页面共享一个弹出组件 | 否决（本轮） | 看片计划海报的预览要加载几十张缩略图，两边的准备步骤不同；只把**复制/下载**这一处抽出来共用 |
| 分享图上手写 focus trap / `role=dialog` | 否决 | 仓库已有 S2 `Dialog` 自带全套（§5） |

## 验收标准

1. 红黑榜控制行出现「生成分享图」→ 打开弹层即出图（预览可见 + 「下载 PNG 图片 / 复制图片」可用）。
2. 图里**三榜各 TOP10**，顺序与页面上点同档位时**一致**；**该榜指标为 0 的片不进该榜**；
   某榜一条都没有时**整节不出现**（不留空节）。
3. 「我贴过的」**不限条数**；每行有「我贴的那一色」（贴纸形状的小圆片）与「全站红黑数」；
   中英名相同时不重复画两行（与页面卡片同一条展示规则）。
4. 底部署名两行：`biff.lcandy.co` 与 `by @gaaiyeoi 和 by @lcandy2`（两个 handle 都是**账号名**）。
5. a11y：`role=dialog` + Esc 关闭 + **焦点归还**给「生成分享图」按钮。
6. 收口：`npm run verify:quick` + 三浏览器跑 `redblack` / `share-export` / `parity-dialogs` 全绿
   （后两个是 `poster-brush.ts` / `share-image.ts` 抽公共层后的回归网）。

## 修订记录

- 2026-09-22 初版。先写 PLAN 再动手。
- 2026-09-22 **修订 1**：署名 handle 用**账号名**，不用提交者显示名。

  用户原话：

  > 新版需要对齐旧版页脚写的是 @lcandy2的链接指向 github.com/lcandy2  @ gaaiyeoi 指向github.com/gaaiyeoi

  **根因**：旧版页脚写 `@lcandy2`；新版页脚（`App.tsx`）写成 `@citron` 但链接仍指向 `github.com/lcandy2`。
  查过 git 作者名，两者是**同一个人**：

  ```
  122 gaaiyeoi <gaaiyeoirao@tencent.com>
   87 gaaiyeoi <gaaiyeoi@gmail.com>
   19 citron   <45784494+lcandy2@users.noreply.github.com>
   17 citron   <citron@users.noreply.github.com>
  ```

  `citron` 是他的 git **提交者显示名**（36 条提交的作者名都是它），`lcandy2` 才是 GitHub **账号名**
  （`45784494+` 那个前缀就是该账号的 user id）。

  **改法**：`App.tsx` 页脚文字 `@citron` → `@lcandy2`（链接不动，本来就是 `github.com/lcandy2`）；
  `redblack-poster.ts::CREDIT_BY` 同步；两处断言（单测 + E2E）同步。
  **为什么这事不能只算「文案微调」**：分享图 / 页面上的 handle 是**给读者去搜的**，
  印一个 GitHub 上不存在的显示名，等于没有署名 —— 而链接（指向账号主页）又会让点进去的人以为写错了。

  🤔 **一处诚实交代**：页脚署名目前**没有 E2E 断言**（`grep gaaiyeoi e2e/` 只有分享图那条）。
  这条串已经漂移过一次（旧版 `@lcandy2` → 新版 `@citron`），值得补一条断言 ——
  但 `App.tsx` 现在是**另一条在途改动**的同一个文件（导出/分享的埋点搬迁），
  本轮只在提交时手工切 hunk 取走页脚这一处，**没有顺手改它的测试归属**。
- 2026-09-22 **修订 2**：分享图上每一行要**看到贴纸**，不只是数字。

  用户原话：

  > 我想在分享页把贴纸也展示出来吧 现在只有数字

  **改法（第一版，被修订 3 取代）**：每行在片名下方加一条**横贯整行的贴纸带**（`BAND_H = 32`）。

  - 落点 / 歪斜**逐字复用卡片画布**那套：`redblack.ts::spotOf` / `tiltOf`，由贴纸 id 确定性推导。
  - 组成也同源：群点 = **别人的**票（`othersOf` —— 这条口径本轮才从 `RbCard` 里抽出来，
    卡片画布与分享图现在共用同一份），我那一枚**追加在末尾**并加一圈亮边
    （与卡片上「我贴的那一枚压在群点之上」同序）。
  - 票数**不打折**（几枚画几枚）；探出边缘按卡片同口径裁掉（`clip()` ↔ `.rb-canvas{overflow:hidden}`）。
  - ⚠ 坐标映射要**收进区内**：卡片画布有 176px 高，26px 的贴纸压不出边界；而这块区只有 88px，
    照搬「中心 = posY × 高」会让上下沿的贴纸被切平（画面上是一排平顶）。
  - 行高 76 → **104**（`ROW_H` 导出给单测，替代原先写死的 `76`）；原先那条红黑比例条**删掉** ——
    贴纸区本身就回答了比例，留着是两条重复的表达。
- 2026-09-22 **修订 3**：「贴纸带」改成**两列 + 专门的贴纸区**。

  用户原话：

  > 为什么不能也沿用两列 然后有一个专门的贴纸绘制区

  **为什么第一版选了横带（当时的取舍）**：76px 的行里只塞得下 30px 高的东西，横贯整行还能顺带
  表达整行的红黑比例。**为什么它不对**：① 页面卡片本来就是「左文字 + 右**画布**」，横带是新造的第
  三种表达，读的人要重新学；② 横带只有 32px 高，纵坐标被压扁成一涂，看着不像「撒上去的贴纸」，
  像一道色块；③ 那条比例条其实已经回答过比例了，横带把它换个方式又说了一遍。

  **改法**：一行改两列 —— 左列文字（名次 / 我那一色 + 片名 + 英文名 + 红黑数），右列
  **`360×88` 的贴纸区**（与卡片画布同色）。行高 104（比第一版还矮 4px：文字占三行、贴纸区吃满高度）。
  贴纸尺寸 14、`FIELD_TOP = 8`，两列间距 `COL_GAP = 28`。
  ⚠ **代价说清楚**：贴纸区的宽度是从文字列手里让出来的（360 + 28），片名的可用宽度从 ~684 降到 **516**
  —— 二十来字的长片名会截断（如英文名 `[Master Class] The Cinematic Visions of Alfonso CU…`）。
  这是两列版式的固有代价：**要么贴纸有地方撒，要么片名一字不省**。
  「我贴过的」那一节的提示语同步：「左侧圆点是我贴的那一色 · 横带是这部片收到的全部贴纸」。

### 附：长分享图在 iPhone 上是一张**空图**（本轮发现并修）

两张分享图原先只用 `SCALE_DOWN_H = 8000` 挡超采样（那是给画布**单边** 32767 留的余量）。
但画布还有一条**面积**上限：**iOS Safari 约 16.7M px**，超了同样 `toBlob` **静默出空图**。
1080×2 = 2160 宽 ⇒ 逻辑高一旦超过 **~3700**（约 20 场行程，或 3 个榜 + 几行「我贴过的」），
2× 就已经越线，而 8000 这条阈值拦不住 —— 用户在 iPhone 上「复制图片」拿到的是一张空图
（`posterBlob` 只判了 `null`，空图不是 null，所以连错误提示都不会有）。

**改法**：`poster-brush.ts` 新增 `MAX_PIXELS = 16_000_000` 与 `posterScale(w, h)`，两张分享图都改用它 ——
**宁可 1×，也不能出空图**。代价：逻辑高 > ~3700 的长图从 2× 降为 1×；
1080 宽的 1× 图在手机上（390pt × 3 ≈ 1170 物理像素）几乎就是 1:1，肉眼差别很小。

**本轮验收**：单测 559 passed（`redblack-poster` 26 条）；`verify:quick` 全绿；
E2E 三浏览器 `redblack` + `share-export` + `parity-dialogs` + `smoke` **96 passed**
（`parity-dialogs` 里那条画布断言 `width: 2160` 是短行程，仍在 2× 档，未被面积上限波及）。

### 修订 4（2026-09-22）：分享哪些节由用户勾选

用户原话：

> 分享的时候 选一下需要分享的长图内容吧 有的人不想所有的榜单都分享

**改法**：弹层里加一个「长图内容」勾选组（S2 `CheckboxGroup`，四个节各一项，**默认全选** ——
「少给」才需要动作）。

- 勾掉的节**连算都不算**（`BOARDS.filter(picked.has)`），不是算完再藏起来：少一节就该少一节的开销。
- 渲染顺序按**固定档位序**（总数 / 红 / 黑 / 我贴过的），不随勾选顺序变。
- 「我贴过的（N 部）」在 0 部时**置灰**（勾出一节空白没有意义）；标题里的条数照旧显示真实值
  —— 没勾不等于「0 部」。
- 一节都不勾 → 预览照旧（能看见真的只剩头部与署名），但**下载 / 复制停用**（免得把一张空图发出去）；
  空态文案分两句：**「还没选」vs「真的没票」** —— 共用一句会把用户引到错的地方找原因。

**E2E 踩到的三个坑（留给下一个人）**：

1. RAC 的复选框真 `<input>` 是**视觉隐藏**的，`uncheck()` 会被上层样式 div 挡掉
   （`intercepts pointer events`）—— 得按用户的做法点**标签文字**（与 `schedule-toolbar` 同手法）。
2. `trackPaintedTexts` 是**累加**的：判断「这一张图上有什么」必须前后各读一次、只比新增段，
   否则永远能看到上一张的残留。
3. **别拿节名当判据** —— 图里那个大标题「**红黑榜**」本身就含「黑榜」三个字，
   `not.toContain("黑榜")` 会永远失败；要用各节的口径说明（「按黑贴纸数」这种）。

### 修订 5（2026-09-22）：出图尺寸提示 + 头部署名

用户原话：

> 提示一下选择不同的分享模块后图片分别的大小是多少; gaaiyeoi 和lcandy 的在头部也加一下就行

**① 尺寸提示**：弹层里在勾选组下面常驻一行「这张长图：1080 × 10392 px · 1× 出图 · PNG 约 3.0 MB」。
宽高走 `rbPosterHeight(model)` + `posterScale()`（**与实际出图同一处判断**，不另算一份），
PNG 大小取 `blob.size`（真的画完了才有）—— 所以是**实测值**，不是估的。

**尺寸只由「行数」决定**：`高 = 520 + Σ(128 + 104 × 该节行数)`
（520 = 上下红条 20 + 头部 396 + 页脚 104；每节固定 128 = 空档 32 + 节头 96，每行 104）。
倍率另一条：`1080 × 高 × 4 > 16M`（高 > 3703）就回 1×。

本机 mock（299 部有排期、三榜各 10 行、「我贴过的 60 部」）**实测**：

| 勾选 | 出图尺寸 | PNG |
|---|---|---|
| 全选（三榜 + 我贴过的 60 部） | 1080 × 10392（1×） | 3.0 MB |
| 三榜 + 我贴过的，去掉黑榜 | 1080 × 9224（1×） | 2.6 MB |
| 只三榜（去掉我贴过的） | 1080 × 4024（1×） | 1.3 MB |
| 只总数榜 + 我贴过的 | 1080 × 8056（1×） | 2.2 MB |
| 只总数榜 / 只红榜 / 只黑榜 | 2160 × 3376（**2×**） | 1.1–1.4 MB |
| 只我贴过的 | 1080 × 6888（1×） | 1.8 MB |
| 一节都不勾 | 2160 × 1040（2×） | 0.3 MB |

⚠ 三榜单开的高度在本机 mock 里一样（每榜都恰好 10 行）；真到票数不足 10 部的榜，行数少 → 更矮。

**② 头部署名**：`drawHeader` 里在届次那一行**右侧**加同一句 `by @gaaiyeoi 和 by @lcandy2`
（与底部一处同源，共用 `model.credit.by`，不占额外高度）—— 长图常被截一半转发，底部那行会跟着丢。
E2E 用 `painted.split(署名).toHaveLength(3)` 断言**出现两次**。
