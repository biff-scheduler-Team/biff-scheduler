# 旧版入口

此目录保存主仓库提交 `8a95215` 的原版界面，访问 `/legacy/`。所有TypeScript源码按该提交原样保留；仅HTML资源路径、返回新版链接和CSS扫描范围适配本项目的多入口构建。

`<base href="/">` 使原有相对JSON请求继续读取站点根目录的同一份数据。新旧版同域，因此直接共享 `biff.*` localStorage，无需导入或迁移副本。

不要把新版React组件或样式引入此入口。修改旧版业务逻辑前应明确对应需求；此目录用于保留可回退的旧版。
